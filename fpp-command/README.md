# FPP Command

FakePlayerPlugin extension that restores `/fpp cmd` outside of the core plugin.

## Command

```text
/fpp cmd <bot> <command>
/fpp cmd <bot> --clear
```

The command is executed as the bot through the FPP API.
